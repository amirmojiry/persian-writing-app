{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the static client shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
