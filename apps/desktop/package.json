{
  "name": "@persian-writing/desktop",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build",
    "check:rust": "cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && cargo check --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml",
    "typecheck": "echo 'Rust type checks run in the desktop workflow'",
    "lint": "echo 'Rust formatting checks run in the desktop workflow'",
    "test": "echo 'SQLite contract tests run in the desktop workflow'"
  },
  "devDependencies": {"@tauri-apps/cli": "^2.2.5"}
}
